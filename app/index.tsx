import Survey from "./survey";

/**
 * Root index screen component.
 * Redirects to the survey screen as the initial entry point of the app.
 */
export default function Index() {
  return <Survey />;
}
