/**
 * Application route paths.
 * Defines all navigation routes used in the app.
 */
export const Routes = {
  survey: "/survey",
  swipe: "/swipe",
  customInput: "/custom-input",
  lazyMode: "/lazy-mode",
} as const;
