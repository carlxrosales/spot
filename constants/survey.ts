/**
 * Survey-related UI constants.
 * Defines thresholds and configuration values for survey behavior.
 */
export const Survey = {
  /**
   * Minimum number of answers required before the "Show my spots" button appears.
   */
  minimumAnswers: 3,
};
